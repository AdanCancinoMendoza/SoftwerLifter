// src/components/Nav.tsx
import React from 'react'; // 👈 Esto ya lo hace un módulo válido

const Nav = () => {
  return (
    <nav>
      {/* Tu navegación aquí */}
    </nav>
  );
};

export default Nav; // 👈 Esto también lo hace un módulo
